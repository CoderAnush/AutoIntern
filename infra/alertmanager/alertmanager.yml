route:
  receiver: 'dev-webhook'
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: 'dev-webhook'
    webhook_configs:
      - url: 'http://alert-receiver:5001/'
        send_resolved: true
